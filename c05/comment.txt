这里补充一点东西
eval这条命令的功能很强大，可以将字符串str当成有效表达式来求值并返回计算结果。
用得厉害感觉可以用程序来生成程序
但是缺点是安全性。
比如要求：输入一个表达式，并求值
假如用户输入：__import__('os').system('dir')
那么eval()之后，你会发现，当前目录文件都会展现在用户前面。
并且如果继续输入：
open('文件名').read()
文件内容似乎也暴露了。
